//@check
// Functions can be declared locally _inside a block_
// and are visible only inside that block.

fun Outer()
{
    InBlock();      //~error
    
    if (true)
    {
        InBlock();          // valid
        fun InBlock()
            = Standard.Print(""); 
    }
    
    InBlock();      //~error
}