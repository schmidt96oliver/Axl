//@check

// Break and continue exprs have type never, because they are
// diverging.

loop
{
        break;
//~type ^^^^^ never
        continue;
//~type ^^^^^^^^ never
}

// Expr return from loop does not change break type.

loop
{
        break 1;
//~type ^^^^^^^ never
}