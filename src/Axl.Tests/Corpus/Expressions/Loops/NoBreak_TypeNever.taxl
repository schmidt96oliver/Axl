//@check

// The loop without break has type never, because
// it loops infinitely.

var a = loop { };
//~type ^^^^^^^^ never

        a;
//~type ^ never

// Continue does not affect the type.

var a = loop { continue; };
//~type ^^^^^^^^^^^^^^^^^^ never